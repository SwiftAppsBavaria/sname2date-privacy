# sName2Date 說明

## 本 App 的用途

sName2Date 會在檔案名稱中尋找日期，並把它當作拍攝日期寫進影像或影片檔案。若檔案還沒有拍攝
日期，就替它建立一個。

完整版為此提供**兩種操作模式**，可在視窗上方切換：

| | |
|---|---|
| **拍攝日期** | 把日期寫進檔案——本 App 的主要工作 |
| **檔案名稱** | 只把名稱改成 ISO 寫法，適用於**任何**檔案 |

## 第一步

1. 透過「選擇檔案…」挑選檔案，或拖到視窗上。完整版也接受整個資料夾連同子資料夾。
2. 列表會顯示每個檔案辨識到的日期，以及（若有的話）已經設定的拍攝日期。
3. 「寫入日期」執行變更。

## 常見問題

**某個檔案顯示「名稱中找不到日期」。**
該名稱不含可辨識的日期。可辨識的寫法包括 `2024-01-15 10-30-00`、`IMG_20240115_103000`、
`2024-01-15`、`2024 01 15`、`2024_01_15`、`15.01.2024`、`15-01-2024`、`15.01.24`，以及像
`January 15 2024` 或 `Jan. 15, 2024` 這類拼寫成文字的月份名稱。`2016 04` 和 `04-2016` 也讀
得到——此時以該月的第一天為準，列表會一併說明。

拼寫成文字的月份名稱，本 App 會以你的系統語言和英文辨識。中文的月份寫法（`1月`）並非由字母
組成的字詞，因此不屬於這一類——中文名稱請改用上面的數字形式，例如 `2024-01-15`。

音譯寫法也讀不到：德文的 `15 Maerz 2024` 用 ae/oe/ue 代替母音變音，就無法辨識；這種情況請
在該列右側手動填入日期。

**某個日期被讀成了錯誤的日。**
4 月 3 日在德文寫作 `3.4.`，在英文寫作 `4/3`——同樣兩個數字，意義卻相反。碰到 `15-03-2024`
並無妨，因為沒有第 15 個月。只有當兩個數字都能當月份時（`03-05-2024`），才必須做決定：這時
列表上方會出現一條橘色列，列出兩種讀法供你選擇，受影響的列也會標示出來。預設值為你系統地
區的寫法。

**名稱中含有兩個日期。**
以第一個為準：在 `IMG_20240115_已編輯_2019-03-02` 中，取的是拍攝的那個，而不是後來的註記。
帶有時間的日期一律優先於不帶時間的。

**名稱中只有日期，沒有時間。**
那麼會採用一個假定的時間——預設為中午 12 點。可在設定中更改。

**我想替一張掃描的老照片標註日期。**
可以：手動填入的日期可回溯到 1826 年，也就是現存最早照片的年份。

**我的照片會不會失真？**
不會。影像資料原封不動地沿用，JPEG 不會重新壓縮。影片的軌道是直接傳遞過去的，不會重新編碼。

**支援哪些格式？**
在「拍攝日期」模式下：影像為 JPEG、PNG、TIFF、HEIC 和 GIF，影片為 MP4、MOV 和 M4V。本 App
會把日期寫進上述所有格式的檔案本身。

在「檔案名稱」模式下**不看副檔名**——那裡只更動名稱，而名稱是每個檔案都有的。

**我的 HEIF、WebP 或 AVI 檔案根本沒出現在列表裡。**
這三種格式無法容納拍攝日期——本 App 只能把日期擱在它們旁邊，而不是寫進去，而那正不是本 App
的用途。因此在「拍攝日期」模式下，它們在載入時會被略過；列表上方會有一列說明有多少個。

它們仍然可以重新命名：切換到「檔案名稱」即可。

遇到 HEIF 時，往往換個副檔名就行：同樣的資料若叫作 `.heic`，就算另一種格式，也就寫得進去。

**我只想整理檔案名稱，不動檔案本身。**
在視窗上方切換到「檔案名稱」。這時本 App 不會開啟任何一個檔案，只更動名稱——`帳單
15.03.2024.pdf` 會變成 `2024-03-15 12-00-00 帳單.pdf`，資料夾在 Finder 中便會依日期排序。
這適用於任何檔案類型，包括 PDF、文字檔或試算表。

若希望日期留在它原本在名稱中的位置，請關閉「日期置於開頭」。

⚠️ 在這個操作模式下，本 App 只接受**資料夾**，不接受個別檔案。原因就出在重新命名本身：它會
更動資料夾項目，而這需要 macOS 對該資料夾的許可——這份許可是在你選取資料夾時產生的。選過一
次之後，本 App 就會記住；「選擇資料夾」按鈕會以選單列出最近用過的資料夾。

**我的檔案旁邊有一個副檔名為 `.xmp` 的檔案。**
那是別的程式留下的——Lightroom 和 digiKam 會建立這類附隨檔案。sName2Date 不會產生它們，但在
更動拍攝日期時會把既有的一併帶上。否則檔案說一套、附隨檔案說另一套，而大多數程式會先讀附隨
檔案。

**我可以還原某項變更嗎？**
可以。⌘Z 會還原一整次執行——拍攝日期、建立日期與修改日期，以及（若已開啟）變更過的檔案名
稱。⌘⇧Z 可再次套用。

不過還是提醒一句：處理大量收藏之前，請先做一份備份。還原功能會把數值復原，但不能取代備份。

**重新命名沒有成功，但日期已經寫進檔案了。**
若選取的是單一檔案，本 App 只能對這個檔案作業，不能在它所在的資料夾裡作業——而重新命名會更
動資料夾項目。請改為選取資料夾而不是個別檔案，或在本 App 詢問時給予授權。授權一次之後，即
使重新啟動仍然有效，也涵蓋所有子資料夾。

## 有東西出錯了？

「設定」→「診斷」→「儲存記錄…」會把最近七天的訊息存成一個文字檔。歡迎連同錯誤描述一併寄
來。

## 聯絡方式

SwiftAppsBavaria · SwiftAppsBavaria@gmx.net
