# sName2Date のヘルプ

## このアプリがすること

sName2Date はファイル名の中から日付を探し、それを撮影日時として画像や動画のファイルに
書き込みます。まだ撮影日時がない場合は、新しく作成します。

製品版にはそのための**二つの動作モード**があり、ウインドウの上部で切り替えられます。

| | |
|---|---|
| **撮影日時** | 日付をファイルに書き込む — このアプリの本来の仕事 |
| **ファイル名** | 名前だけを ISO の表記にする。**どんな**ファイルでも対象 |

## 最初の手順

1. 「ファイルを選択…」でファイルを選ぶか、ウインドウにドラッグします。製品版はサブ
   フォルダを含むフォルダ全体も受け付けます。
2. リストには、ファイルごとに認識された日付と、すでに設定されている撮影日時があれば
   それが表示されます。
3. 「日付を書き込む」で変更を実行します。

## よくある質問

**あるファイルに「名前に日付が見つかりません」と表示されます。**
その名前には、日付として認識できる表記が含まれていません。認識されるのは、たとえば
`2024-01-15 10-30-00`、`IMG_20240115_103000`、`2024-01-15`、`2024 01 15`、`2024_01_15`、
`15.01.2024`、`15-01-2024`、`15.01.24` などです。文字で綴られた月名では
`January 15 2024` のような英語の形が読み取られます。`2016 04` と `04-2016` も読まれ、
その場合はその月の1日とみなされ、行にその旨が表示されます。

文字で綴られた月名は、システムの言語と英語で認識されます。日本語の月は「1月」のように
数字で書き、文字の並びではないため、日本語の月名は対象になりません。ドイツ語の
`15 Maerz 2024` のように、ウムラウトを ae/oe/ue に置き換えた綴りも認識されません。
その場合は、行の右側で日付を手入力してください。

**日付が違う日として読まれます。**
4月3日はドイツ語で `3.4.`、英語では `4/3` と書きます。同じ二つの数字が逆の意味を
持つわけです。`15-03-2024` なら問題はありません。15 という月は存在しないからです。
両方の数字が月として通る場合（`03-05-2024`）だけ、決める必要があります。そのときは
リストの上にオレンジ色の帯が現れ、二つの読み方から選べるようになり、該当する行には印が
付きます。初期設定はお使いの地域の表記です。

**名前に日付が二つあります。**
最初のものが選ばれます。`IMG_20240115_bearbeitet_2019-03-02` なら、後からの書き込みでは
なく撮影のほうです。時刻を伴う表記は、時刻のない表記よりも常に優先されます。

**名前に日付だけがあり、時刻がありません。**
その場合は時刻が補われます。初期設定は正午12時です。設定で変更できます。

**スキャンした古い写真に日付を付けたいのですが。**
できます。手で入力する日付は、現存する最古の写真が撮られた 1826 年までさかのぼれます。

**写真の画質は落ちますか。**
いいえ。画像データはそのまま引き継がれ、JPEG が再圧縮されることはありません。動画では
トラックがそのまま渡され、再エンコードは行われません。

**対応している形式は何ですか。**
*撮影日時* の動作モードでは、画像は JPEG、PNG、TIFF、HEIC、GIF、動画は MP4、MOV、M4V
です。これらすべてに、アプリは日付をファイル自体へ書き込みます。

*ファイル名* の動作モードでは**拡張子は関係ありません**。そこでは名前だけが変わり、
名前はどのファイルにもあるからです。

**HEIF、WebP、AVI のファイルがリストにまったく現れません。**
この三つの形式は撮影日時を保持できません。アプリは日付をその傍らに置くことしかできず、
それはこのアプリの目的ではありません。そのため *撮影日時* の動作モードでは読み込みの
際に除かれ、リストの上の行に、その数が表示されます。

名前の変更なら可能です。そのためには *ファイル名* に切り替えてください。

HEIF の場合は、拡張子だけで解決することがよくあります。同じデータでも `.heic` という
名前なら別の形式として扱われ、書き込みが行われます。

**ファイルには触れずに、ファイル名だけを整えたいのですが。**
ウインドウの上部で *ファイル名* に切り替えてください。するとアプリはファイルを一つも
開かず、名前だけを変更します。`請求書 15.03.2024.pdf` は
`2024-03-15 12-00-00 請求書.pdf` になり、フォルダは Finder で日付順に並びます。これは
PDF、テキスト、表計算を含め、あらゆる種類のファイルに当てはまります。

日付を名前の中にあった位置に残したい場合は、「日付を先頭に置く」をオフにしてください。

⚠️ この動作モードでは、アプリは**フォルダ**だけを受け付け、個々のファイルは受け付け
ません。理由は名前の変更そのものにあります。名前の変更はフォルダの項目を書き換えるため、
macOS はフォルダに対する許可を必要とし、その許可はフォルダを選ぶことで生じます。一度
選べばアプリはそれを覚えており、「フォルダを選択」のボタンが最近使ったものをメニューに
並べます。

**ファイルの隣に拡張子 `.xmp` のファイルがあります。**
それは別のプログラムによるものです。Lightroom や digiKam がそうした付属ファイルを作り
ます。sName2Date は付属ファイルを作りませんが、撮影日時を変更するときには、すでにある
ものを一緒に更新します。そうしないとファイルと付属ファイルが食い違ったことを言うことに
なり、多くのプログラムは付属ファイルのほうを先に読むからです。

**変更を元に戻せますか。**
はい。⌘Z は一度の実行を丸ごと取り消します。撮影日時、作成日と変更日、そして有効に
していれば変更したファイル名もです。⌘⇧Z でやり直せます。

とはいえ一言。大きなコレクションを処理する前には、バックアップを取ってください。取り消し
は値を元に戻しますが、バックアップの代わりにはなりません。

**名前の変更はうまくいきませんでしたが、日付はファイルに入っています。**
個々のファイルを選んだ場合、アプリはそのファイルに対してだけ作業でき、そのフォルダの中で
は作業できません。そして名前の変更はフォルダの項目を書き換えます。個々のファイルではなく
フォルダを選ぶか、アプリが尋ねてきたときに許可を与えてください。一度与えた許可は再起動
後も有効で、すべてのサブフォルダにも及びます。

## うまくいかないときは

設定 → 診断 → 「ログを保存…」で、直近7日間のメッセージをテキストファイルに書き出せます。
不具合の説明と一緒に送っていただければ幸いです。

## 連絡先

Andreas Heiligtag · andreas.heiligtag@gmx.de
