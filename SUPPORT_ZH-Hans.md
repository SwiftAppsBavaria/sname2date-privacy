# sName2Date 帮助

## App 做什么

sName2Date 在文件名中查找日期，并把它作为拍摄日期写入图片或影片文件。文件中还没有拍摄日期时，就
新建一个。

完整版为此有**两种工作模式**，在窗口上方切换：

| | |
|---|---|
| **拍摄日期** | 把日期写入文件——App 的主要任务 |
| **文件名** | 只把名称改成 ISO 写法，适用于**任意**文件 |

## 第一步

1. 用「选择文件…」挑出文件，或者把它拖到窗口上。完整版也接收整个文件夹连同子文件夹。
2. 列表会为每个文件显示识别出的日期，以及已经设置好的拍摄日期（如果有）。
3. 「写入日期」执行更改。

## 常见问题

**某个文件显示「名称中未找到日期」。**
名称里没有可识别的日期。可识别的写法包括 `2024-01-15 10-30-00`、`IMG_20240115_103000`、
`2024-01-15`、`2024 01 15`、`2024_01_15`、`15.01.2024`、`15-01-2024`、`15.01.24`，以及写成文
字的月份名称，例如 `15 Jan. 2024` 或 `January 15 2024`。`2016 04` 和 `04-2016` 也读得出来——
这时取当月的第一天，该行上会写明这一点。

月份名称，App 按你的系统语言和英文来识别。中文里的月份写作 `1月`，不是由字母组成的词，无法作为月
份名称识别——中文文件名请用上面的数字写法。转写形式也识别不了，例如德语的 `15 Maerz 2024`（用
ae/oe/ue 代替变音符号）；这种情况请在该行右侧手动填写日期。

**日期被读成了错误的日子。**
4 月 3 日在德语里写作 `3.4.`，在英语里写作 `4/3`——同样两个数字，含义相反。遇到 `15-03-2024` 无
所谓，第 15 个月并不存在。只有两个数字都能当月份讲时（`03-05-2024`）才必须做决定：这时列表上方会
出现一条橙色横条，让你在两种读法之间选择，受影响的行也会被标出来。默认采用你所在系统区域的写法。

**名称里有两个日期。**
第一个为准：在 `IMG_20240115_已编辑_2019-03-02` 中，取的是拍摄日期，而不是后来的备注。带时间的日
期始终优先于不带时间的。

**名称里只有日期，没有时间。**
那就假定一个时间——默认是中午 12 点。可以在设置中修改。

**我想给一张扫描进来的老照片标注日期。**
可以：手动填写的日期一直回溯到 1826 年，也就是现存最早照片的年份。

**我的照片会损失画质吗？**
不会。图像数据原样保留，JPEG 不会重新压缩。影片的轨道是直接传递的，不会重新编码。

**支持哪些格式？**
在*拍摄日期*模式下：图片为 JPEG、PNG、TIFF、HEIC 和 GIF，影片为 MP4、MOV 和 M4V。对这些格式，App
都把日期写入文件本身。

在*文件名*模式下，**扩展名不重要**——那里只改名称，而名称每个文件都有。

**我的 HEIF、WebP 或 AVI 文件在列表里根本不出现。**
这三种格式容纳不下拍摄日期——App 只能把日期放到文件旁边，而不是写进去，而这不是它该做的事。因此在
*拍摄日期*模式下，它们在载入时会被略过；列表上方有一行说明有多少个。

重命名倒是可以：切换到*文件名*模式即可。

对 HEIF，改扩展名往往就够了：同样的数据叫作 `.heic` 就是另一种格式，那样就写得进去了。

**我只想整理文件名，不想动文件本身。**
在窗口上方切换到*文件名*。这时 App 一个文件也不打开，只改名称——`发票 15.03.2024.pdf` 变成
`2024-03-15 12-00-00 发票.pdf`，文件夹在 Finder 里就按日期排好了。这适用于任何种类的文件，PDF、
文本或表格也一样。

想让日期留在名称中原来的位置，就把「日期置于开头」关掉。

⚠️ 在这种工作模式下，App 只接收**文件夹**，不接收单个文件。原因就在重命名本身：它改动的是文件夹
条目，而 macOS 为此需要针对该文件夹的许可——这个许可是通过你选择文件夹产生的。选过一次之后，App
就记住了；「选择文件夹」按钮会在菜单里列出最近用过的文件夹。

**我的文件旁边有一个扩展名为 `.xmp` 的文件。**
它来自别的程序——Lightroom 和 digiKam 会建立这样的伴随文件。sName2Date 不会生成，但在更改拍摄日期
时会把已有的一并带上。否则文件说一套、伴随文件说另一套，而大多数程序会先读伴随文件。

**我可以撤销更改吗？**
可以。⌘Z 撤销整整一次操作——拍摄日期、创建日期和修改日期，以及（如果开启了）改动过的文件名。
⌘⇧Z 再做一遍。

但还是提醒一句：处理大批收藏之前，请先做一次备份。撤销能还原这些值，但代替不了备份副本。

**重命名没有成功，可日期已经写进文件里了。**
如果选中的是单个文件，App 就只许在这个文件上工作，不能在它所在的文件夹里工作——而重命名改动的正是
文件夹条目。请选择文件夹而不是单个文件，或者在 App 询问时给予授权。授权一次之后，重启也依然有效，
并且连同所有子文件夹一起覆盖。

## 出了问题？

设置 → 诊断 →「保存日志…」会把最近七天的消息写进一个文本文件。欢迎连同问题描述一起发来。

## 联系方式

Andreas Heiligtag · andreas.heiligtag@gmx.de
