# sName2Date 帮助

## App 做什么

sName2Date 在文件名中查找日期，并把它作为拍摄日期写入图片、影片或音频文件。文件中还没有拍摄日期时，就新建一个。

为此，窗口上方有两个复选框，二者并不互斥：

| | |
|---|---|
| **把日期写入文件** | 把日期作为拍摄日期写进文件本身——App 的主要任务 |
| **调整文件名** | 把名称改成 ISO 写法，适用于**任何**种类的文件 |

两个都勾选是常规用法。只勾选第二个时，App 一个文件也不打开，只改名称。

## 第一步

1. 用「选择文件…」挑出文件，或者把它拖到窗口上。完整版也接收整个文件夹连同子文件夹。
2. 列表会为每个文件显示识别出的日期，以及已经设置好的拍摄日期（如果有）。
3. 「写入日期」执行更改。

## 常见问题

**某个文件显示「名称中未找到日期」。**
名称里没有可识别的日期。可识别的写法包括 `2024-01-15 10-30-00`、`IMG_20240115_103000`、
`2024-01-15`、`2024 01 15`、`2024_01_15`、`15.01.2024`、`15-01-2024`、`15.01.24`，以及写成文字的月份名称，例如 `15 Jan. 2024` 或 `January 15 2024`。`2016 04` 和 `04-2016` 也读得出来——
这时取当月的第一天，该行上会写明这一点。

月份名称，App 按你的系统语言和英文来识别。中文里的月份写作 `1月`，不是由字母组成的词，无法作为月份名称识别——中文文件名请用上面的数字写法。转写形式也识别不了，例如德语的 `15 Maerz 2024`（用
ae/oe/ue 代替变音符号）；这种情况请在该行右侧手动填写日期。

**日期被读成了错误的日子。**
4 月 3 日在德语里写作 `3.4.`，在英语里写作 `4/3`——同样两个数字，含义相反。遇到 `15-03-2024` 无所谓，第 15 个月并不存在。只有两个数字都能当月份讲时（`03-05-2024`）才必须做决定：这时列表上方会出现一条橙色横条，让你在两种读法之间选择，受影响的行也会被标出来。默认采用你所在系统区域的写法。

**名称里有两个日期。**
第一个为准：在 `IMG_20240115_已编辑_2019-03-02` 中，取的是拍摄日期，而不是后来的备注。带时间的日期始终优先于不带时间的。

**名称里只有日期，没有时间。**
那就假定一个时间——默认是中午 12 点。可以在设置中修改。

**我想给一张扫描进来的老照片标注日期。**
可以：手动填写的日期一直回溯到 1826 年，也就是现存最早照片的年份。

**我的照片会损失画质吗？**
不会。图像数据原样保留，JPEG 不会重新压缩。影片的轨道是直接传递的，不会重新编码。

**支持哪些格式？**
App 会把拍摄日期写入文件本身的有：图片（JPEG、PNG、TIFF、HEIC、GIF）、影片（MP4、MOV、M4V）和录音（M4A、M4B）——这时该行的勾号是绿色的。

不过列表接收**任何**文件。如果格式容纳不下拍摄日期——比如 PDF、文本文件或表格——App 就改为设置文件的创建日期和修改日期；这时勾号是橙色的。应用程序、替身和包格式的文稿不会出现在列表里。

**我的 HEIF、WebP 或 AVI 文件的勾号是蓝色的。**
这三种格式容纳不下拍摄日期。因此 App 把它写进一个同名、扩展名为 `.xmp` 的伴随文件，Lightroom 或
digiKam 等照片程序会一并读取它。

对 HEIF，改扩展名往往就够了：同样的数据叫作 `.heic` 就是另一种格式，那样就能写进文件本身了。

**我只想整理文件名，不想动文件本身。**
在窗口上方关掉「把日期写入文件」，打开「调整文件名」。这时 App 一个文件也不打开，只改名称，以及（如果在设置中开启了）创建日期和修改日期——`发票 15.03.2024.pdf` 变成
`2024-03-15 12-00-00 发票.pdf`，文件夹在 Finder 里就按日期排好了。这适用于任何种类的文件，PDF、文本或表格也一样。

想让日期留在名称中原来的位置，就把「日期置于开头」关掉。

重命名改动的是文件夹条目，而 macOS 为此需要针对该文件夹的许可。如果你在完整版中直接选择文件夹，许可随之就有了。如果选的是单个文件（在 sName2Date Lite 中总是如此），App 会询问一次文件夹——选一个上级文件夹也可以，而且这项授权重启后依然有效。在完整版中，「选择文件夹」按钮会在菜单里列出最近用过的文件夹。

**我的文件旁边有一个扩展名为 `.xmp` 的文件。**
它要么来自别的程序（Lightroom 和 digiKam 会建立这样的伴随文件），要么是 sName2Date 建立的，因为该格式本身容纳不下拍摄日期（HEIF、WebP、AVI；这时勾号是蓝色的）。这样建立的伴随文件可以用 ⌘Z 撤销。已有的伴随文件，App 在更改拍摄日期时会一并带上。否则文件说一套、伴随文件说另一套，而大多数程序会先读伴随文件。

**文件夹很大时，App 会询问是否继续载入。**
这只涉及完整版，sName2Date Lite 不接受文件夹。文件数达到 5000 个时——例如连同子文件夹载入整个用户文件夹——它会暂停并询问。载入和分析期间，它会显示计数和进度条；列表完整之后才能写入。换一个小一些的文件夹，或关掉「包含子文件夹」，会更快。

**我可以撤销更改吗？**
可以。⌘Z 撤销整整一次操作——拍摄日期、创建日期和修改日期，以及（如果开启了）改动过的文件名。
⌘⇧Z 再做一遍。

但还是提醒一句：处理大批收藏之前，请先做一次备份。撤销能还原这些值，但代替不了备份副本。

**重命名没有成功，可日期已经写进文件里了。**
如果选中的是单个文件，App 就只许在这个文件上工作，不能在它所在的文件夹里工作——而重命名改动的正是文件夹条目。请选择文件夹而不是单个文件，或者在 App 询问时给予授权。授权一次之后，重启也依然有效，并且连同所有子文件夹一起覆盖。

## 出了问题？

设置 → 诊断 →「保存日志…」会把最近七天的消息写进一个文本文件。欢迎连同问题描述一起发来。

## 联系方式

SwiftAppsBavaria · SwiftAppsBavaria@gmx.net
