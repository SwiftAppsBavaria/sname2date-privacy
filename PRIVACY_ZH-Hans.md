# sName2Date 隐私政策

版本日期：2026-08-22

## 简要说明

sName2Date **不会**收集、存储或传输任何个人数据。App 只在你的 Mac 上工作，不建立任何互联网连接。

## App 处理哪些数据

sName2Date 只读取你明确交给它的文件——通过打开对话框选择，或者拖到窗口上。读取的是文件名和文件的
元数据；写入的是拍摄日期，而且只写入这些文件。

需要的话，App 还会为这些文件重命名，并设置它们的创建日期和修改日期。这两项都默认关闭，须由你明确
开启。

在**文件名**工作模式下，App **一个文件也不打开**：它只读取名称，也只修改名称。文件内容既不读取也
不写入。

没有你的选择，App 不会访问任何文件。macOS 通过 App 沙盒强制执行这一点。

## App 在你的 Mac 上留下什么

- **设置**，保存在 App 受保护的专属文件夹中的 `config.json` 文件里。
- **一份诊断日志**，位于同一区域，七天后自动删除。其中记录的是操作的时间点和数量。你可以通过
  设置 → 诊断把它保存下来并转交出去；除此之外，它不会离开你的 Mac。
- **你授权过的文件夹的路径**，连同 macOS 的许可，以便下次启动时重新打开它们。只有这样，App 才不
  必每次都重新询问。这个列表中只有文件夹路径，没有文件内容，「选择文件夹」按钮会把它显示给你。

删除 App 时，这些内容都会随 App 一起被清除。

**除了你的文件之外，App 不会新建任何东西。**如果文件旁边已经有一个扩展名为 `.xmp` 的伴随文件——
它来自别的图像程序——，其中的拍摄日期会一并更新，以免文件和伴随文件说法不一。App 看不懂的内容，
保持原样不动。

## 不转交，不分析

没有广告，没有分析服务，没有向第三方发送的崩溃报告，也没有账户。

## 联系方式

Andreas Heiligtag · SwiftAppsBavaria@gmx.net
