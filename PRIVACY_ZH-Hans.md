# sName2Date 隐私政策

版本日期：2026-09-25

本隐私政策同时适用于 sName2Date 和 sName2Date Lite。

## 简要说明

sName2Date **不会**收集、存储或传输任何个人数据。App 只在你的 Mac 上工作，不建立任何互联网连接。

## App 处理哪些数据

sName2Date 只读取你明确交给它的文件——通过打开对话框选择，或者拖到窗口上。读取的是文件名和文件的元数据；写入的是拍摄日期，而且只写入这些文件。

需要的话，App 还会为这些文件重命名（默认关闭）。此外，它还会设置它们的创建日期和修改日期（默认开启，可在设置中关闭）。对于无法承载拍摄日期的文件（例如 PDF 或文本），它始终只设置这两个日期。

如果未勾选**把日期写入文件**，App **一个文件也不打开**：它只读取名称，也只修改名称，以及（如已设置）创建日期和修改日期。文件内容既不读取也不写入。

没有你的选择，App 不会访问任何文件。macOS 通过 App 沙盒强制执行这一点。

App 浏览某个文件夹时，如果其中包含「音乐」文件夹，macOS 可能会询问是否允许它访问「媒体与Apple Music」。
App 既不读取你的资料库，也不读取你的播放记录。它在那里也和在其他地方一样只处理文件，并把日期写入名称中带有日期的音频和视频文件。

## App 在你的 Mac 上留下什么

- **设置**，保存在 App 受保护的专属文件夹中的 `config.json` 文件里。
- **一份诊断日志**，位于同一区域，七天后自动删除。其中记录的是操作的时间点和数量。你可以通过设置 → 诊断把它保存下来并转交出去；除此之外，它不会离开你的 Mac。
- **你授权过的文件夹的路径**，连同 macOS 的许可，以便下次启动时重新打开它们。只有这样，App 才不必每次都重新询问。这个列表中只有文件夹路径，没有文件内容，「选择文件夹」按钮会把它显示给你。

删除 App 时，这些内容都会随 App 一起被清除。

**只有一种情况下，App 会在你的文件旁边新建东西**：如果照片、视频或音频文件本身无法容纳拍摄日期（例如 HEIF、WebP 或 AVI），App 会把它写入一个同名、扩展名为 `.xmp` 的伴随文件，许多照片程序都会一并读取这种文件。⌘Z 可以将其撤销。在其他文件（例如 PDF 或文本）旁边，永远不会生成伴随文件。如果文件旁边已经有一个伴随文件——它来自别的图像程序——，其中的拍摄日期会一并更新，以免文件和伴随文件说法不一。App 看不懂的内容，保持原样不动。

## 不转交，不分析

没有广告，没有分析服务，没有向第三方发送的崩溃报告，也没有账户。

## 联系方式

Andreas Heiligtag · SwiftAppsBavaria@gmx.net
