# sName2Date のプライバシーポリシー

最終更新: 2026-09-24

## 要約

sName2Date は個人に関わるデータを**一切**収集も保存も送信しません。このアプリはあなたの
Mac の中だけで動作し、インターネットに接続することはありません。

## アプリが扱うデータ

sName2Date は、あなたが明示的に渡したファイルだけを読みます。開くダイアログでの選択、またはウインドウへのドラッグによって渡されたものです。読み取るのはファイル名とファイルのメタデータで、書き込むのはまさにそのファイルへの撮影日時です。

必要に応じて、アプリはそれらのファイルの名前も変更します（初期状態ではオフ）。さらに、作成日と変更日も設定します（初期状態ではオン、設定でオフにできます）。撮影日時を持てないファイル — たとえば PDF やテキスト — では、代わりに常にこの二つの日付だけを設定します。

**日付をファイルに書き込む**のチェックがオフのとき、アプリは**ファイルを一つも開きません**。名前だけを読み、名前と、設定されていれば作成日と変更日だけを変更します。その際、内容は読まれも書かれもしません。

あなたの選択がなければ、アプリはどのファイルにもアクセスしません。macOS が App
サンドボックスによってこれを守らせます。

「ミュージック」フォルダが入っているフォルダをアプリが調べると、macOS が「メディアとApple Music」へのアクセスを許可するかどうか尋ねることがあります。アプリはあなたのライブラリも再生履歴も読みません。そこでも他の場所と同じくファイルだけを扱い、名前に日付が含まれるオーディオファイルとビデオファイルにその日付を書き込みます。

## アプリが Mac 上に置くもの

- **設定** — アプリ専用の保護されたフォルダにある `config.json` というファイル。
- **診断のための記録** — 同じ場所にあり、7日後に自動的に削除されます。処理の時刻と件数が記録されます。設定 → 診断 から保存して渡すことができます。それ以外にあなたの Mac から出ることはありません。
- **あなたが許可したフォルダのパス** — 次回の起動時にそれらを再び開くための macOS の許可とともに保存されます。そのおかげで、アプリは毎回改めて尋ねずに済みます。この一覧にあるのはフォルダのパスだけで、ファイルの内容は含まれません。「フォルダを選択」のボタンから内容を確認できます。

これらはすべて、アプリを削除すると一緒に取り除かれます。

**アプリがあなたのファイルの傍らに何かを作るのは、一つの場合だけです。** 写真、ビデオ、オーディオのファイルが撮影日時をそれ自体に保持できない場合（たとえば HEIF、WebP、AVI）、アプリはそれを同じ名前で拡張子 `.xmp` の付属ファイルに書き込みます。この付属ファイルは多くの写真アプリが併せて読み取ります。⌘Z で元に戻せます。それ以外のファイル、たとえば PDF やテキストの傍らには決して作られません。付属ファイルがすでにそこにある場合 — それは別の画像プログラムによるものです —、ファイルと付属ファイルが食い違ったことを言わないように、その撮影日時も併せて更新されます。アプリが理解しない内容はそのまま残ります。

## 第三者への提供も分析もありません

広告も、分析サービスも、第三者へのクラッシュレポートも、アカウントもありません。

## 連絡先

Andreas Heiligtag · SwiftAppsBavaria@gmx.net
