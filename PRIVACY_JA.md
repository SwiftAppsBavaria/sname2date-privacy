# sName2Date のプライバシーポリシー

最終更新: 2026-08-22

## 要約

sName2Date は個人に関わるデータを**一切**収集も保存も送信しません。このアプリはあなたの
Mac の中だけで動作し、インターネットに接続することはありません。

## アプリが扱うデータ

sName2Date は、あなたが明示的に渡したファイルだけを読みます。開くダイアログでの選択、
またはウインドウへのドラッグによって渡されたものです。読み取るのはファイル名とファイルの
メタデータで、書き込むのはまさにそのファイルへの撮影日時です。

必要に応じて、アプリはそれらのファイルの名前を変更し、作成日と変更日も設定します。
どちらも初期状態ではオフで、自分で明示的に有効にするものです。

**ファイル名**の動作モードでは、アプリは**ファイルを一つも開きません**。名前だけを読み、
名前だけを変更します。その際、内容は読まれも書かれもしません。

あなたの選択がなければ、アプリはどのファイルにもアクセスしません。macOS が App
サンドボックスによってこれを守らせます。

## アプリが Mac 上に置くもの

- **設定** — アプリ専用の保護されたフォルダにある `config.json` というファイル。
- **診断のための記録** — 同じ場所にあり、7日後に自動的に削除されます。処理の時刻と件数が
  記録されます。設定 → 診断 から保存して渡すことができます。それ以外にあなたの Mac から
  出ることはありません。
- **あなたが許可したフォルダのパス** — 次回の起動時にそれらを再び開くための macOS の許可
  とともに保存されます。そのおかげで、アプリは毎回改めて尋ねずに済みます。この一覧に
  あるのはフォルダのパスだけで、ファイルの内容は含まれません。「フォルダを選択」の
  ボタンから内容を確認できます。

これらはすべて、アプリを削除すると一緒に取り除かれます。

**アプリはあなたのファイルの傍らに新しいものを作りません。** 拡張子 `.xmp` の付属ファイル
がすでにそこにある場合 — それは別の画像プログラムによるものです —、ファイルと付属ファイル
が食い違ったことを言わないように、その撮影日時も併せて更新されます。アプリが理解しない
内容はそのまま残ります。

## 第三者への提供も分析もありません

広告も、分析サービスも、第三者へのクラッシュレポートも、アカウントもありません。

## 連絡先

Andreas Heiligtag · andreas.heiligtag@gmx.de
